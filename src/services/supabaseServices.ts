/**
 * @deprecated Renamed to `consoleApiServices.ts` (M-03).
 * Despite the legacy name, this service layer communicates exclusively with the
 * authenticated Owner OS REST backend API via apiClient.
 * All functions are canonicalized in `consoleApiServices.ts`.
 */
export * from './consoleApiServices';
